import React from "react";

const Content = ({ data }) => {
  return "sd";
  // <ul>
  //   {data.map((obj, index) => (
  //     <li key={index}>{JSON.stringify(obj)}</li>
  //   ))}
  // </ul>
};

export default Content;
